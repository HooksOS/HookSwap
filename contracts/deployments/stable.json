{
  "chain": "stable",
  "chainId": 988,
  "deployer": "0xc14C897c6bff88a5Eeac31F795693b9230205125",
  "weth9": "TBD (deploy own WETH9 — routing wrapper only; native=USDT0, wrappedNativeCurrency stays null)",
  "weth9Note": "Stable Mainnet has NO canonical wrapped-native. Deploy a fresh WETH9 (contracts/scripts/deploy-weth.sh) used ONLY as the WETH9 constructor arg for v2 router / v3 periphery / SwapRouter02 / UniversalRouter. Native gas token is USDT0 (18-dec native balance). The interface keeps stable.ts wrappedNativeCurrency = null (Tempo/Arc model). This WETH9 is never held by users.",
  "gasToken": {
    "address": "0x779Ded0c9e1022225f8E0630b35a9b54bE713736",
    "symbol": "USDT0",
    "decimals": 6,
    "note": "USDT0 ERC-20 on 988 — VERIFIED symbol 'USDT0', decimals 6, NOT a wrapped-native (no deposit()/withdraw()). Used for gas-token-override / balances / routing (Arc-USDC model). The NATIVE gas balance is 18-decimal USDT0; this 6-dec ERC-20 is the routable token."
  },
  "permit2": "0x000000000022D473030F116dDEE9F6B43aC78BA3",
  "permit2Note": "canonical CREATE2 deployment, present on 988, reused (do NOT deploy)",
  "multicall3": "0xcA11bde05977b3631167028862bE2a173976CA11",
  "multicallNote": "canonical Multicall3 (present on 988, reused) — this is NOT the deploy-v3 Multicall2. The @uniswap/deploy-v3 CLI deploys its own Multicall2 during the v3 step; record that one in `multicall2` below.",
  "v2Factory": "TBD",
  "v2FactoryTx": "TBD",
  "v2FactoryNote": "Deploy canonical UniswapV2Factory(feeToSetter=deployer) via forge-create. Deterministic same-as-other-chains address (0xD1Cf66..6B45) ONLY if deployer nonce is 0 on 988; if nonce != 0 the address shifts (see tempo).",
  "v2Router02": "TBD",
  "v2Router02Tx": "TBD",
  "v2PairInitCodeHash": "0x96e8ac4277198ff8b6f785478aa9a39f403cb768dd02cbee326c3e7da348845f",
  "v3Factory": "TBD",
  "multicall2": "TBD",
  "proxyAdmin": "TBD",
  "tickLens": "TBD",
  "nftDescriptorLibrary": "TBD",
  "nftPositionDescriptor": "TBD",
  "descriptorProxy": "TBD",
  "nonfungiblePositionManager": "TBD",
  "v3Migrator": "TBD",
  "v3Staker": "TBD",
  "quoterV2": "TBD",
  "swapRouter02": "TBD",
  "swapRouter02Tx": "TBD",
  "poolInitCodeHash": "0xe34f199b19b2b4f47f68442619d555527d244f78a3297ea89325f843f87b8b54",
  "universalRouter": "TBD",
  "universalRouterTx": "TBD",
  "universalRouterNote": "Deploy the HooksOS universal-router fork with 11-field RouterParameters: permit2=canonical, weth9=own WETH9 (step 1), v2Factory, v3Factory, pairInitCodeHash + poolInitCodeHash = the two canonical hashes above, v4PoolManager/permissionsAdapterFactory/v3NFTPositionManager/v4PositionManager/spokePool = address(0). Verify supportedURVersions (_2_0) matches at interface wire-up time.",
  "gasNote": "Native gas = USDT0 (18-dec native balance). Network gas price ~1.13 gwei. deploy-v3 CLI --gas-price is integer-gwei → `--gas-price 1` (or 2) is fine here (unlike the sub-gwei L2s that needed the fractional-gwei patch). If Stable applies a Tempo-style contract-creation gas multiplier, costs scale up — confirm on-chain before broadcast.",
  "_status": "PENDING — awaiting USDT0 gas funding on 988; all addresses TBD until broadcast"
}
