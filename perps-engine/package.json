{
  "name": "hookswap-perps-engine",
  "version": "0.1.0",
  "private": true,
  "description": "HookSwapPerps off-chain matching-engine support — pluggable oracle registry (scalar mark price from AMM v2/v3/v4 + external Chainlink/Pyth/API feeds for RWA/stocks). Add markets/venues by config + adapter module, no Settlement redeploy.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "oracle:demo": "node --loader ts-node/esm oracle/demo.ts"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "ts-node": "^10.9.2",
    "@types/node": "^22.0.0"
  }
}
