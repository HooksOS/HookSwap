# Pinned-ish top-level deps mirroring pyproject.toml (use `pip install -e .` or this file).
# Web / API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
strawberry-graphql[fastapi]>=0.235.0
python-multipart>=0.0.9
websockets>=12.0

# Data / persistence
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
redis>=5.0.0
pgvector>=0.3.0

# Vector / search
qdrant-client>=1.9.0
opensearch-py>=2.6.0
rank-bm25>=0.2.2

# LLM / embeddings
openai>=1.40.0
anthropic>=0.34.0
google-generativeai>=0.7.0
httpx>=0.27.0
tiktoken>=0.7.0

# Marketing X (Twitter) bot
tweepy>=4.14.0          # X API v2 posting (x_client falls back to httpx+OAuth1 if absent)
Pillow>=10.0.0          # branded image-card PNG rasterizer (falls back to SVG if absent)

# Utils
numpy>=1.26.0
tenacity>=8.4.0
structlog>=24.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
orjson>=3.10.0

# Dev / test (install with: pip install -r requirements.txt)
pytest>=8.2.0
pytest-asyncio>=0.23.0
aiosqlite>=0.20.0
voyageai>=0.3.0
